/**
 * Maintenance items define the schedule (intervals) per vehicle.
 * Last-done and next-due are derived from service records at query time
 * via computeMaintenanceItemsWithDue in utils/helpers/maintenance.ts,
 * used by the dashboard for recommended services.
 */

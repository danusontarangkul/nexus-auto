import React from 'react';
import { Screen } from '@/shared/components/Screen';
import { SectionHeader } from '@/shared/components/SectionHeader';

export function RecordsScreen() {
  return (
    <Screen>
      <SectionHeader title="Records" variant="titleLg" />
    </Screen>
  );
}
